// import { Navbar } from "./Navbar";

export const Layout = ({ children }) => {
  return (
    <>
      <header>header</header>
      {/* <Navbar /> */}
      {children}
      <footer>footer</footer>
    </>
  );
};
