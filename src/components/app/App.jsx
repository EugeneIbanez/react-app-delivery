import { Layout } from "../layout/Layout";
import { RestaurantList } from "../restaurants/RestaurantList";

export const App = () => {
  return (
    <Layout>
      <RestaurantList />
    </Layout>
  );
};
